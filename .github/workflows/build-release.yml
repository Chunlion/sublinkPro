name: Build and Release Multi-Platform

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Personal fork notice
        run: |
          echo "The upstream release workflow is disabled in this personal fork."
          echo "Use the Build Custom Docker Image workflow for GHCR image builds."
