name: PR Checks

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Personal fork notice
        run: |
          echo "PR checks are disabled in this personal fork."
          echo "This repository is maintained for personal use only."
